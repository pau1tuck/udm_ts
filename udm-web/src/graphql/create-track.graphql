mutation CreateTrack($input: TrackInput!) {
    createTrack(input: $input) {
        id
        artist
        title
        version
        label
        image
        filename
        createdAt
        updatedAt
        votes
    }
}
