mutation CreateTrack($input: TrackInput!) {
    createTrack(input: $input) {
        id
        artist
        title
        version
        label
        month
        year
        youTubeId
        buyUrl
        createdAt
        updatedAt
        votes
    }
}
