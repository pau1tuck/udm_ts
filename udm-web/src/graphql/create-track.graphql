mutation CreateTrack($input: TrackInput!) {
    createTrack(input: $input) {
        id
        trackId
        artist
        title
        version
        label
        month
        year
        buyUrl
        createdAt
        updatedAt
        votes
    }
}
