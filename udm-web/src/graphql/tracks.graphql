query Tracks($limit: Int!) {
    tracks(limit: $limit) {
        payload {
            id
            trackId
            artist
            title
            version
            label
            month
            year
            buyUrl
            votes
            createdAt
            updatedAt
        }
        hasMore
    }
}
